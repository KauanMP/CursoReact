// Css
import styles from "./Modal";

interface Props {}

const Modal = (props: Props) => {
  return (
    <div id="modal">
      <div></div>
      <div>
        <h2>texto modal</h2>
      </div>
    </div>
  );
};

export default Modal;
